/**
 * 
 * LMResponse class - A general HTTP Response class for LMOS for Node.js project
 * 
 */

import {Response, ResData} from './lib/Response.js';
import {ResError} from './lib/ResError.js';

export { Response as LMResponse, ResData as LMResponseData, ResError as LMResponseError };
